# Online play (M4). Copy to .env and fill in from the Supabase dashboard
# (Project Settings → API). The anon key is publishable — all authority
# lives behind RLS and the edge function.
EXPO_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=YOUR-ANON-KEY
