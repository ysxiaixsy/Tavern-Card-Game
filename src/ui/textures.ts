/**
 * Generated material tiles (npm run gen:textures — scripts/gen-textures.ts).
 * Seamless, baked around the token palette; tile with resizeMode:'repeat'.
 */

export const TEXTURE = {
  /** App/table background: near-black oak with vertical grain. */
  oakDark: require('../../assets/textures/oak_dark.png') as number,
  /** Raised wood for bars/strips/chips. */
  oakMid: require('../../assets/textures/oak_mid.png') as number,
  /** Panel/sheet/deck-card leather. */
  leather: require('../../assets/textures/leather.png') as number,
  /** Aged paper (light surfaces, placeholder card faces). */
  parchment: require('../../assets/textures/parchment.png') as number,
};
